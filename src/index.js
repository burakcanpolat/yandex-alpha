import langbase from './config/langbase.js';

async function main() {
    try {
        // Test Langbase connection
        console.log('Testing Langbase connection...');
        // Add your test code here once you have the API key set up
        
        console.log('Langbase connection successful!');
    } catch (error) {
        console.error('Error connecting to Langbase:', error.message);
    }
}

main();